name: Daily Newsletter

on:
  schedule:
    - cron: '0 6 * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4

      - run: pip install feedparser requests beautifulsoup4 groq
      - run: python newsletter.py
